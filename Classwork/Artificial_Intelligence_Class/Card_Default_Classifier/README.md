This project is a simple Decision Tree classifier project that leverages the AI Python code 
(found here: https://artint.info/AIPython/) to determine if someone will default on their card
based on data found in the carddata.csv file. The model_writeup.txt file showcases my decisions
as to how to train the model. 